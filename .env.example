# Prisma (Day Trader). Use a path on persistent storage in production.
DATABASE_URL=file:./dev.db
# Bazaar SQLite path (persistent storage in production)
# DATABASE_PATH=./server/data/bazaar.db
SESSION_SECRET=change-me
